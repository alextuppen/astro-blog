---
import { Root } from "../layouts";
import { Button, Section } from "../components";

import {
  URL_LINKEDIN,
  URL_INSTAGRAM,
  URL_GITHUB,
  SITE_DESCRIPTION,
} from "../consts";
---

<Root title={"Alex Tuppen"} description={SITE_DESCRIPTION}>
  <Section className="root">
    <div class="grid">
      <img src="/logo/white.svg" alt="Alex Tuppen logo" class="at-logo" />
      <div class="name-wrapper">
        <h1 class="name">Alex</h1>
        <h1 class="name">Tuppen</h1>
      </div>
      <h2 class="tag-line">Full stack developer</h2>
      <div class="buttons">
        <Button variant="large-icon" href={URL_LINKEDIN} external={true}>
          <img
            width="48"
            height="48"
            src="/icons/websites/linkedin.svg"
            alt="LinkedIn logo"
          />
        </Button>
        <Button variant="large-icon" href={URL_INSTAGRAM} external={true}>
          <img
            width="48"
            height="48"
            src="/icons/websites/instagram.svg"
            alt="Instagram logo"
          />
        </Button>
        <Button variant="large-icon" href={URL_GITHUB} external={true}>
          <img
            width="48"
            height="48"
            src="/icons/websites/github.svg"
            alt="Github logo"
          />
        </Button>
      </div>
    </div>
  </Section>
</Root>

<style lang="scss">
  @import "../styles/_globals.scss";

  .root {
    // 100 small view height - (2 x double padding (4) on section + rough height of header (3))
    height: calc(100svh - (7 * var(--padding)));
  }

  .test {
    display: flex;
  }

  .grid {
    display: grid;
    grid-template-areas:
      "at-logo"
      "name-wrapper"
      "tag-line"
      "buttons";
    justify-items: center;
    align-items: center;
    gap: var(--padding-double);
  }

  .at-logo {
    grid-area: at-logo;
    width: 10rem;
  }

  .name-wrapper {
    grid-area: name-wrapper;
    display: flex;
    flex-direction: column;
  }

  .name {
    margin: auto;
    font-size: 4rem;
    line-height: 1;
  }

  .tag-line {
    grid-area: tag-line;
    font-size: 1.5rem;
    font-weight: 500;
    letter-spacing: 0.25rem;
  }

  .buttons {
    grid-area: buttons;
    display: flex;
    gap: var(--padding-double);
  }

  @include bp-small {
    .grid {
      grid-template-areas:
        "at-logo name-wrapper"
        "tag-line tag-line"
        "buttons buttons";
    }

    .name {
      margin: 0;
      font-size: 6rem;
    }

    .tag-line {
      font-size: 2.25rem;
      letter-spacing: 0.5rem;
    }
  }

  @include bp-medium {
    .at-logo {
      width: 12rem;
    }

    .name {
      font-size: 7rem;
    }

    .tag-line {
      font-size: 2.5rem;
      letter-spacing: 0.75rem;
    }
  }

  @include bp-large {
    .at-logo {
      width: 16rem;
    }

    .name {
      font-size: 9rem;
    }

    .tag-line {
      font-size: 3rem;
      letter-spacing: 1rem;
    }
  }

  @include bp-xl {
    .at-logo {
      width: 20rem;
    }

    .name {
      font-size: 12rem;
    }

    .tag-line {
      font-size: 3.75rem;
      letter-spacing: 1.5rem;
    }
  }
</style>
